import { LastNamePipePipe } from './last-name-pipe.pipe';

describe('LastNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LastNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
